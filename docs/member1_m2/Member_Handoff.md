# Continuing team integration

The initial lifecycle trace is complete, with actual source, schema, decisions and verification linked in Product_Trace.md and the RTM. The M1 and A2 contributions are reconciled against this implementation.

The wider M2 submissions remain owned by their authors. Final integration remains outstanding until the architecture/design diagrams, application schema/interface artefacts and full stack/deployment comparison are received. Each submission must be mapped to the same requirement IDs, checked against acceptance criteria and reconciled with the PED, RTM, ADRs, risks and implementation evidence. Conflicts require a controlled change before integration.

The reference slice does not replace those separate submissions. Any different final technology or design is recorded with rationale and affected paths; do not overwrite previous decisions or claim the reference tests automatically cover a different application.
